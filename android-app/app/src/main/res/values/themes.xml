<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.KarurSdo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- System splash: India Post mark on warm off-white; hands off to the Compose splash. -->
    <style name="Theme.KarurSdo.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.KarurSdo</item>
    </style>
</resources>
